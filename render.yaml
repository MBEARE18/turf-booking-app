services:
  # Backend Web Service
  - type: web
    name: turf-booking-backend
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    region: ohio
    plan: free
    rootDir: backend
    envVars:
      - key: NODE_VERSION
        value: 18.0.0
      - key: PORT
        value: 5000
      - key: MONGO_URI
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: RAZORPAY_KEY_ID
        sync: false
      - key: RAZORPAY_KEY_SECRET
        sync: false
      - key: GOOGLE_SCRIPT_URL
        sync: false

  # Frontend Static Site
  - type: static
    name: turf-booking-frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    rootDir: frontend
    envVars:
      - key: VITE_API_URL
        value: https://turf-booking-backend.onrender.com/api
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
